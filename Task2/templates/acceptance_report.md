# Task2 验收报告

## 基本信息

- 组别：
- 日期：
- 小车编号：
- 训练设备：

## 核心验收

| 类别 | 验收项 | 实测值或证据 | 结果 |
| --- | --- | --- | --- |
| 环境 | Ubuntu 22.04 运行在 WSL2 |  |  |
| 环境 | `drobot-train` Conda 环境可从 YAML 重新创建 |  |  |
| 环境 | Python 为 3.10.x，且来自当前 Conda 环境 |  |  |
| 环境 | PyTorch、OpenCV、ONNX、ONNX Runtime 和交互标注脚本可用 |  |  |
| 环境 | Task2 环境不依赖 ROS 2 |  |  |
| 数据 | MP4、同名 JSON 和录制清单完整匹配 |  |  |
| 数据 | 每张图片可追溯至视频、帧号和时间 |  |  |
| 数据 | ROI 统一为 `frame[256:480, 0:640]` |  |  |
| 标注 | 正、负和拒绝样本均使用规定格式 |  |  |
| 标注 | 坐标无越界，正式样本无异常 NaN 或 Inf |  |  |
| 划分 | 同一视频不跨 train、val 和 test |  |  |
| 划分 | 跨集合近似重复帧为 0 |  |  |
| 训练 | 小数据过拟合测试通过，正式训练配置和日志完整 |  |  |
| 评估 | 冻结 test 集具有定量指标和失败案例 |  |  |
| ONNX | `onnx.checker` 通过，输入输出符合模型契约 |  |  |
| ONNX | 100 张 test 图片完成 PyTorch/ONNX 一致性验证 |  |  |
| ONNX | 最大输出绝对差 ≤ `1e-4`，坐标差 ≤ `0.1 px` |  |  |
| ONNX | 推理结果中 NaN 和 Inf 数量为 0 |  |  |

结果填写：`通过`、`不通过` 或 `不适用`。

## 数据集与训练结果

### 数据量

| Split | 视频数 | 正样本 | 负样本 | 拒绝样本 |
| --- | ---: | ---: | ---: | ---: |
| train |  |  |  |  |
| val |  |  |  |  |
| test |  |  |  |  |

### 训练信息

- 最终配置：
- 最佳 epoch：
- 最佳模型：
- 数据集 manifest SHA256：
- 训练耗时：

### 模型指标

| 指标 | train | val | test |
| --- | ---: | ---: | ---: |
| x MAE / px |  |  |  |
| y MAE / px |  |  |  |
| 二维距离 MAE / px |  |  |  |
| 二维距离 P90 / px |  |  |  |
| 20 px 内命中率 |  |  |  |
| 置信度 Precision |  |  |  |
| 置信度 Recall |  |  |  |
| 置信度 F1 |  |  |  |

### ONNX 一致性

| 检查项 | 实测值 |
| --- | --- |
| 实际测试图片数 |  |
| 最大原始输出绝对差 |  |
| 最大坐标差 / px |  |
| NaN/Inf 数量 |  |
| 报告文件 |  |

### 失败案例

- 最大误差场景：
- 误检场景：
- 漏检场景：
- 光照问题：
- 弯道问题：
- 下一轮数据改进计划：

## 学习总结与思考

### 1. 任务边界与训练环境

- Task1 和 Task2 的职责边界是什么？
- 为什么训练数据和训练输出不建议放在 `/mnt/c`？
- 回答与实测证据：

### 2. 数据集制作

- 为什么不直接使用视频中的全部帧？抽帧频率会怎样影响数据重复度？
- 正样本、负样本和拒绝样本有什么区别？它们分别如何参与损失计算？
- 为什么同一段视频不能同时出现在 train 和 test？
- 回答与实测证据：

### 3. 模型训练与评估

- 为什么先用少量图片验证模型能够过拟合？
- 为什么坐标损失只对 `has_track=1` 的样本计算，而置信度损失使用全部样本？
- MAE 较小但 P90 很大说明了什么？请结合本组的最大误差图片分析。
- 为什么验证集和测试集不使用随机数据增强？
- 回答与实测证据：

### 4. ONNX 与下一轮改进

- 为什么 ONNX 和 PyTorch 输出允许存在微小误差？本次最大误差是多少？
- 为什么 ONNX 数值误差合格，但测试样本不足时整体报告仍可能显示失败？
- 当前模型最容易在哪些场景失败？下一轮应该补采什么数据？
- 回答与实测证据：

## 问题排查记录

至少记录一个实际问题；如果本次没有故障，记录一次主动检查过程。

| 现象或风险 | 定位过程 | 原因 | 解决方法 | 下次如何避免 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## Task3 交接

- [ ] `line_follower_resnet18.onnx`
- [ ] `model_contract.yaml`
- [ ] `train_config.yaml`
- [ ] `environment.yml`
- [ ] `environment-lock.yml`
- [ ] `requirements-lock.txt`
- [ ] `dataset_card.md`
- [ ] `evaluation_report.md`
- [ ] `onnx_parity_report.json`

## 最终结论

- [ ] 通过
- [ ] 有条件通过
- [ ] 未通过

- 附加条件或未通过项：
