# Task1 验收报告

## 基本信息

- 组别：
- 日期：
- 小车编号：

## 核心验收

| 类别 | 验收项 | 实测值或证据 | 结果 |
| --- | --- | --- | --- |
| 摄像头 | `/image` 为压缩图像，28～30 FPS |  |  |
| 摄像头 | `/image_hbmem` 为 NV12 推理流，28～30 FPS |  |  |
| 摄像头 | `/image_show` 为预览流，约 15 FPS |  |  |
| Foxglove | Bridge 可连接，能查看 `/image_show` 和 `/odom` |  |  |
| 遥操 | WASD、组合转向和方向键调参正常 |  |  |
| 遥操 | 持续按键不产生连续字符，状态信息仍可见 |  |  |
| 安全 | 松键、`P`、`Esc` 和正常退出均停车 |  |  |
| 录制 | 视频来自 `/image`，可播放且同名元数据完整 |  |  |

结果填写：`通过`、`不通过` 或 `不适用`。

## 完整任务与视频

| 任务 | 最低采集要求 | 实际次数 | 代表视频 | 结果 |
| --- | --- | ---: | --- | --- |
| 任务一 | 完整路线至少 3 遍 |  |  |  |
| 任务二 | 顺时针、逆时针各至少 2 遍 |  |  |  |
| 任务三 | 完整路线至少 3 遍 |  |  |  |

完整视频清单填写在随报告提交的 `recording_manifest.csv`。

## 学习总结与思考

### 1. 三路图像流

- `/image`、`/image_hbmem` 和 `/image_show` 分别服务于什么环节？
- 为什么不使用同一路图像完成训练、推理和远程预览？
- 改变摄像头分辨率会怎样影响网络、CPU、VPU 和存储？
- 回答与实测证据：

### 2. 远程观察与遥操

- Foxglove Desktop、Foxglove Bridge 和 `DRobotTeleop` 分别承担什么职责？
- 回答与实测证据：

### 3. Ackermann 转向

- 为什么单独按 `A/D` 不会让车辆原地转向？
- 实际转向使用了什么按键和速度指令组合？
- 回答与实测证据：

### 4. 训练数据

- 为什么训练视频录制 `/image`，而不是 `/image_show`？
- MP4、同名 JSON 和 `recording_manifest.csv` 分别为 Task2 提供什么信息？
- 回答与实测证据：

## 问题排查记录

至少记录一个实际问题；如果本次没有故障，记录一次主动检查过程。

| 现象或风险 | 定位过程 | 原因 | 解决方法 | 下次如何避免 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 最终结论

- [ ] 通过
- [ ] 有条件通过
- [ ] 未通过

- 附加条件或未通过项：
